import React from 'react'; 


/*
export interface IRow { 
  id:number; 
  entry:IEntry; 
} 

export interface IColumn {} 


export interface IField { 
  accessor: string; 
  label: string; 
  type: string; 
  subtype: string; 
  modeltype: string; 
  options: any; 
  defaultValue: any; 
  format: any; 

  cellMode:{ 
    read:(value:any) => string, 
    edit:(value:any, setValue:any) => string, 
    //hover: defaultDisplay, 
  } 
} 

export interface ICollection {
  accessor:string; 
  label: string; 
  fields: Array<IField>; 
  entries: Array<IEntry>; 
}

export interface ICrudTable { 
  Create: (entry:IEntry) => Promise<void>; 
  Update: (entry:IEntry) => Promise<void>; 
  Delete: (entry:IEntry) => Promise<void>; 
} 
*/